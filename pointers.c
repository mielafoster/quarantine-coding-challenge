#include <stdio.h>

int main(){
  int number = 10;
  /*We denote the address of the integer with the %p symbol*/
  printf("This is the addess of the integer %p", &number);
  return 0;
}

/*Another variation of this is declaring the pointer*/

int main(){
  int number = 10;

  int *p;
  /*Where p is the address of the number*/

  p =
  
  printf("This is the addess of the integer %p", p);
  return 0
}
